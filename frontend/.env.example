# Frontend environment variables
# Copy to .env.local and fill with your Supabase project values

REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# API base for notices and other serverless endpoints
# For local UI development without running serverless functions locally,
# you can point to the production deployment, e.g.:
# REACT_APP_API_URL=https://smartcityos-yourid-parichay.vercel.app/api
# Otherwise leave it unset to use relative '/api' when served together.

