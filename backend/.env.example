# Backend local development
# If you don't have Postgres locally, start the server in no-DB mode:
FALLBACK_NO_DB=true
NODE_ENV=development
PORT=3030

# If you have Postgres, fill any of the following. DATABASE_URL takes precedence.
# DATABASE_URL=postgres://user:password@localhost:5432/smart_city_os
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_city_os
DB_USER=postgres
DB_PASSWORD=password

# SSL options (for managed DBs like Neon). Usually not needed locally.
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false

# CORS origin for WebSocket and API
REACT_APP_API_URL=http://localhost:3000

